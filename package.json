{
  "name": "monohanzi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "PUBLIC_BASE_PATH=/ astro dev",
    "build": "astro build",
    "build:local": "PUBLIC_BASE_PATH=/ astro build",
    "preview": "PUBLIC_BASE_PATH=/ astro preview",
    "validate:glyphs": "node scripts/validate-glyphs.mjs",
    "test": "npm run validate:glyphs && npm run build"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}

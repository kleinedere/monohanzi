# Monohanzi

A quiet, visual Chinese character learning site. The first implementation slice
starts with `家` as an inspectable character page.

## Commands

```sh
npm install
npm run dev
npm run validate:glyphs
npm run build
```

The Astro app is configured for static output under `/monohanzi/` so it can be
deployed to GitHub Pages.
